# LeetCode-Introduction-to-Pandas

##Exercise1: 
### I imported the pandas library for data manipulations;
### I created a function that takes the input as a 2D List and returns a table (DataFrame);
### Formed the columns and gave them to pd.DataFrame so it can assembly the table;
### And returned de result;
